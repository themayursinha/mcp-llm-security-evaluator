# MCP LLM Security Evaluator - Environment Variables Configuration
# Copy this file to .env and update with your values
# The .env file is gitignored and will not be committed to version control

# =============================================================================
# Required Variables (for production use)
# =============================================================================
# At least one API key is required if not using the mock provider

# OpenAI API Key (required if using OpenAI provider)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key (required if using Anthropic provider)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# =============================================================================
# Optional Configuration Variables
# =============================================================================

# Logging Configuration
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log file path (relative to project root)
LOG_FILE=logs/evaluator.log

# Enable log rotation (true/false)
LOG_ROTATION=true

# Maximum log file size in MB before rotation
LOG_MAX_SIZE=10

# Number of backup log files to keep
LOG_BACKUP_COUNT=5

# =============================================================================
# LLM Configuration
# =============================================================================

# Default LLM model to use
# Examples: gpt-3.5-turbo, gpt-4, claude-3-sonnet-20240229
DEFAULT_MODEL=gpt-3.5-turbo

# Maximum tokens for LLM responses
MAX_TOKENS=1000

# =============================================================================
# Report Configuration
# =============================================================================

# Report format: json, html, or both
REPORT_FORMAT=both

# Minimum security score threshold (0-100)
# Evaluations below this score will exit with status code 1
SECURITY_THRESHOLD=70

# =============================================================================
# Security Best Practices
# =============================================================================
# - Never commit .env file to version control
# - Use environment variables in CI/CD pipelines instead of .env files
# - Rotate API keys regularly
# - Use least-privilege access for API keys
# - Monitor API usage and costs
